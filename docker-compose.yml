version: '3.3'

services:
    web:
        build: 
            dockerfile: Dockerfile
            context: .
        image: webassembly:latest
        container_name: webassembly
        
        volumes:
            - ./:/home/web 
        
        network_mode: "host"

        restart: always

        command: tail -f /dev/null