version: '3.3'

services:
    web:
        build: 
            dockerfile: Dockerfile
            context: .
        image: webassembly:latest
        container_name: webassembly
        
        volumes:
            - .:/home/web 

        ports:
            - 8888:8888

        restart: always

        command: tail -f /dev/null